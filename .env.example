# Provider API keys (required for remote providers)

# For Fireworks (default remote provider)
FIREWORKS_API_KEY=sk-xxxx

# For OpenAI (alternative remote provider)
# OPENAI_API_KEY=sk-xxxx

# For Ollama (local provider option)
# No keys needed for Ollama, but requires local installation

# Docs package location (optional, can be specified as command-line argument)
DOCS_PKG_PATH=./docs-llm-pkg

# Embedding model configuration (optional, defaults shown)
# Options: BAAI/bge-small-en-v1.5, BAAI/bge-base-en-v1.5, BAAI/bge-large-en-v1.5, all-MiniLM-L6-v2, all-mpnet-base-v2
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
# FORCE_EMBEDDING_MODEL=true  # Uncomment to force using the specified model instead of LlamaStack's default